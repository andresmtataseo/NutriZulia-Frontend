import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class InstitutionsService {

  constructor() { }

  // TODO: Implement institution-related methods
  // getInstitutions(): Observable<Institution[]>
  // getInstitutionById(id: string): Observable<Institution>
  // createInstitution(institution: Institution): Observable<Institution>
  // updateInstitution(id: string, institution: Institution): Observable<Institution>
  // deleteInstitution(id: string): Observable<void>
}